# 算法与AI基础题目汇总（仅题目）
> 共 29 题（简答 2 道 + 选择 27 道），涵盖数据结构、算法、Python、机器学习、大模型等方向。

---
## 一、简答题

### 第 1 题
简述大模型 Agent 的核心组成组件，并分别说明各组件的核心功能。

### 第 2 题
对比分析智能体 Agent 与 Workflow 固定工作流的核心差异与各自适用场景。

---
## 二、选择题

### 第 1 题
元素 1, 9, 2, 5, 6, 7, 4, 3, 8 依次进入栈 G，9 个元素的出栈顺序为 1, 8, 3, 4, 7, 6, 5, 2, 9，在进栈操作时允许出栈操作，则栈 G 的容量至少为（）
A. 6
B. 7
C. 8
D. 9

---
### 第 2 题
以下关于分类任务评价指标的公式，错误的是（）
A. 召回率 Recall = TP / (TP + FN)
B. 准确率 Precision = TP / (TP + FP)
C. F1-score = 2TP / (2TP + FP + FN)
D. F1-score = (准确率 × 召回率) / (准确率 + 召回率)

---
### 第 3 题
以下算法中，属于强化学习的是（）
A. 支持向量机（SVM）
B. 线性回归
C. K-Means
D. Q-Learning

---
### 第 4 题
以下关于拓扑排序的说法，正确的是（）
A. 有向无环图的拓扑排序序列是唯一的
B. 拓扑排序可用于无向图的最短路径求解
C. 拓扑排序的第一个结点是入度最大的结点
D. 拓扑排序仅适用于有向无环图，常用于表达任务之间的依赖先后关系

---
### 第 5 题
以下不属于时序（序列）模型的是（）
A. LSTM
B. RNN
C. CNN
D. GRU

---
### 第 6 题
以下哪个模块用于为神经网络引入非线性能力？（）
A. 卷积函数
B. 随机梯度下降（SGD）
C. 池化层
D. ReLU 激活函数

---
### 第 7 题
以下哪个属于轻量级、可嵌入的开源向量数据库，常用于 RAG 场景？（）
A. Redis
B. ChromaDB
C. SQLite
D. MySQL

---
### 第 8 题
大模型在某些特定类型输入上表现欠佳，以下哪种解决方案最为合适？（）
A. 使用更大的通用数据集
B. 在训练数据中增加这种特定类型的输入样本
C. 改变模型架构
D. 使用更大参数量的模型

---
### 第 9 题
已知一棵二叉树的前序遍历序列为 `A B D C E F`，中序遍历序列为 `B D A E C F`，则该二叉树的后序遍历序列为：
A. `B D E F C A`
B. `D B F E C A`
C. `D B E C F A`
D. `D B E F C A`

---
### 第 10 题
在样本数量较少但特征维度很高（高维小样本）的场景下，以下哪种模型最为适用？
A. 线性回归
B. 支持向量机
C. 深度学习网络
D. 决策树

---
### 第 11 题
以下关于 Python 中 `*args` 和 `**kwargs` 的说法，错误的是：
A. `**kwargs` 用于接收关键字参数
B. `**kwargs` 会将传入的关键字参数保存为字典
C. `*args` 用于接收可变数量的位置参数
D. 在函数定义中，`**kwargs` 可以写在 `*args` 之前

---
### 第 12 题
一个拥有 1B（10 亿）参数的大模型，使用 bf16 精度存储权重，仅考虑模型权重本身，推理时大约需要多少显存？
A. 1G
B. 4G
C. 2G
D. 8G

---
### 第 13 题
以下算法中，属于强化学习（或大模型强化学习对齐）算法的有：
A. PPO
B. KTO
C. LoRA
D. DPO

---
### 第 14 题
关于二叉树的重建，以下说法正确的有：
A. 前序遍历和后序遍历总能唯一确定任意一棵二叉树
B. 层序遍历和中序遍历可用于唯一重建二叉树
C. 后序遍历和中序遍历可唯一确定一棵二叉树
D. 前序遍历和中序遍历可唯一确定一棵二叉树

---
### 第 15 题
关于 A* 搜索算法，以下说法正确的有：
A. 若启发函数不满足可采纳条件（高估真实代价），会破坏 A* 的最优性保证
B. 一致（相容）启发函数满足三角不等式：h(n) ≤ c(n,n') + h(n')
C. 当 h(n) ≡ 0 时，A* 算法等价于 Dijkstra（一致代价搜索）
D. 可采纳启发函数的定义是永远不会高估节点到目标的真实剩余代价，即 h(n) ≤ h*(n)

---
### 第 16 题
关于程序性能优化与缓存，以下说法正确的有：
A. 减少不必要的对象分配有助于降低运行时开销
B. 链表的随机访问一定比数组更快
C. 分块处理（Tiling）可以改善缓存命中率
D. 连续数组访问通常比随机指针跳转具有更好的局部性

---
### 第 17 题
以下哪些属于 Python 的内置数据类型？
A. bool
B. int
C. float
D. char

---
### 第 18 题
以下哪些属于大模型中常用的位置编码技术？
A. RoPE（旋转位置编码）
B. Sinusoidal（正弦位置编码）
C. 正弦余弦位置编码
D. ALiBi（线性偏置位置编码）

---
### 第 19 题
在检索增强生成（RAG）中，以下哪项不属于重排序（Re-Ranking）模块的作用？
A. 对模型最终生成结果进行评分
B. 生成最终答案
C. 对检索结果按相关性进行重新排序
D. 增加检索到的文档的数量

---
### 第 20 题
关于并查集（Union-Find）数据结构，以下说法正确的有：
A. 路径压缩与按秩合并后，均摊时间复杂度接近 O(α(n))（阿克曼函数反函数，近乎常数）
B. Kruskal 最小生成树算法中使用并查集来判断是否形成环
C. 普通并查集不支持在线删边操作
D. 并查集不适合处理集合合并与连通性查询问题

---
### 第 21 题
堆（Heap）可以在 O(n) 时间内完成初始建堆（build-heap）。
A. False
B. True

---
### 第 22 题
动态规划问题必须使用递归实现，否则就不属于动态规划。
A. False
B. True

---
### 第 23 题
Python 字典（dict）的 key 必须是不可变（可哈希）类型。
A. False
B. True

---
### 第 24 题
红黑树的高度为 O(log n)，因此查找、插入、删除操作的时间复杂度均为 O(log n)。
A. False
B. True

---
### 第 25 题
多模态大模型只能处理文本和图像两种模态。
A. False
B. True

---
### 第 26 题
树状数组（Binary Indexed Tree）的单点更新和前缀和查询操作的时间复杂度均为 O(log n)。
A. False
B. True

---
### 第 27 题
贪心算法每一步都做出局部最优选择，因此一定能得到全局最优解。
A. False
B. True
